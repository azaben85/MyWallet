import 'package:flutter/material.dart';

class ExpenseLineScreen extends StatelessWidget {
  const ExpenseLineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
